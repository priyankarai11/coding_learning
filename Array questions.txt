1. Check if array is sorted

const str = [1, 3, 2, 4, 5];
const res = str.every((ele, index) => index === 0 || ele >= str[index - 1]);
console.log(res); // false

------------------------------------------------------------------------------------------
2. check missing number

let arr = [1,2,4,6,8]
const res=[]
let missing=arr[0]
for(let i=0;i<arr.length;i++){
    if(arr[i]>missing){
       res.push(missing)
       missing++;
    }
    missing+=1
   
}
console.log(res)

----------------------------------------------------------------------

3. Flatten an array

const num=[1, [2, 3], [4, [5, 6]]]
let result=[]
function flatten(input){
    for(let i of input){
        if(Array.isArray(i)){
            flatten(i)
        }else{
            result.push(i)
        }
    }
    return result
}

console.log(flatten(num))

------------------------------------------------------------------

4. Find missing elements in two arrays

const num1=[1,2,3,4]
const num2=[2,3,5,6]

function nonInclude(a,b){
    return a.filter(ele=>!b.includes(ele))
}
console.log([...nonInclude(num1,num2),...nonInclude(num2,num1)])

-----------------------------------------------------------------------------------------------

5. Product of array except self

const num=[1, 2, 3, 4]
const result=[]

for(let i=0;i<num.length;i++){
const copy=[...num]
  copy.splice(i,1)
   const res=copy.reduce((acc,cur)=>acc*cur,1)
   result.push(res)
}
console.log(result)

-------------------------------------------------------------------------------

6. Find all subarrays

const num=[1, 2, 3]
const res=[]
for(let i=0;i<num.length;i++){
    for(let j=i;j<num.length;j++){
        res.push(num.slice(i,j+1))
    }
}
console.log(res)

----------------------------------------------------------------------------------

7. Find longest increasing subarray

const arr=[5, 6, 3, 5, 7, 8, 9, 1]
let res=[arr[0]]
let longest=[arr[0]]
for(let i=1;i<arr.length;i++){
    if(arr[i]>arr[i-1]){
        res.push(arr[i])
    }else{
        res=[arr[i]]
    }
    if(res.length>longest.length){
    longest=res
}
}

console.log(longest) //[ 3, 5, 7, 8, 9 ]
--------------------------------------------------------------------------------------
8. Array sum divisible by K (subarray sum problem)

let arr = [4, 5, 0, -2, -3, 1], k= 5
let v;
const p=[]
for(let i=0;i<arr.length;i++){
    for(let j=i;j<arr.length;j++){
        v=arr.slice(i,j+1)
        const res=v.reduce((acc,curr)=>acc+curr,0)
        if(res%k===0){
            p.push(v)
        }
    }
}
console.log(p)

---------------------------------------------------------------------------------------
9. Find all pairs with difference K

let arr = [1, 7, 5, 9, 2, 12, 3], k = 2
let v;
const p=[]
for(let i=0;i<arr.length;i++){
    for(let j=i+1;j<arr.length;j++){
        const v=Math.abs(arr[i]-arr[j])
        if(v===k){
            p.push([arr[i],arr[j]])
        }
    }
}
console.log(p)
-----------------------------------------------------------------------
10. Maximum product subarray

let arr = [2,3,-2,4]
let max=arr[0]
let result=[]
for(let i=0;i<arr.length;i++){
    let product=1
    for(let j=i;j<arr.length;j++){
        product*=arr[j]
        if(product>max){
            max=product
        }
    }
}
console.log(max)

-------------------------------------------------------------------
11. Maximum sum subarray (Kadane’s Algorithm)

let arr = [-2,1,-3,4,-1,2,1,-5,4]
let max=arr[0]
let p=[]
for(let i=0;i<arr.length;i++){
    for(let j=i;j<arr.length;j++){
        let v=arr.slice(i,j+1)
        p.push(v.reduce((acc,curr)=>acc+curr,0))
    }
}
console.log(Math.max(...p))

-------------------------------------------------------------------------

12. Find majority element (> n/2 times)

let arr = [2, 2, 1, 1, 2, 2, 3]
let n=arr.length/2
let res=[]
const obj={}
for(let i of arr){
    obj[i]=(obj[i] || 0) +1
}

for(let i in obj){
    if(obj[i]>n){
        res.push(Number(i))
    }
}
console.log(res)
----------------------------------------------------------------------------
13. Count inversions in array

let arr = [2,4,1,3,5]
let count=0
for(let i=0;i<arr.length;i++){
    for(let j=i+1;j<arr.length;j++){
         if(arr[i]>arr[j]){
        count++
    }
    }
   
}
console.log(count)

------------------------------------------------------------------------
14. Find kth largest element

let arr = [7, 10, 4, 3, 20, 15],k = 3

const res=arr.sort((a,b)=>b-a)
console.log(res[k-1])

--------------------------------------------------------------------
15. Find longest consecutive elements

let arr=[100,4,200,1,3,2]

const res=arr.sort((a,b)=>a-b)
let first=[res[0]]
let longest=[]

for(let i=1;i<res.length;i++){
     if (res[i] === res[i - 1]) continue;
   if(res[i-1]+1===res[i]){
       first.push(res[i])
   }else{
       if(res[i] !== res[i - 1]){
           first=[res[i]]
       }
   }
    if (first.length > longest.length) longest = first;
}
console.log(longest) //[ 1, 2, 3, 4 ]
---------------------------------------------------------
16. Rotate matrix 90 degrees

const arr=[[1,2,3],[4,5,6],[7,8,9]]
const result=[]
for(let i=0;i<arr.length;i++){
    const newArr=[]
    for(let j=arr.length-1;j>=0;j--){
        newArr.push(arr[j][i])
    }
    result.push(newArr)
}

console.log(result) // [ [ 7, 4, 1 ], [ 8, 5, 2 ], [ 9, 6, 3 ] ]
---------------------------------------------------------------------------

17. Check if array is palindrome

const arr=[1,2,3,4,5]
const res=arr.every((ele,index)=>arr[arr.length-1-index]===ele)
console.log(res)

-----------------------------------------------------------------------------

18. Rotate array left by K positions
let arr=[1,2,3,4,5,8], k=2

let n=k%arr.length

const res=arr.slice(n).concat(arr.slice(0,n))
console.log(res)

---------------------------------------------------------------
19. Smallest missing positive number

let arr=  [-5, -2, 0, 1].sort((a,b)=>a-b);
 let missing=1
for(let i=0;i<arr.length;i++){
   if(arr[i]===missing){
       missing++
   }
}

console.log(missing)
---------------------------------------------------------------------------
20. spiral matrix

function spiralOrder(matrix) {
  let result = [];

  while (matrix.length) {
    
    result.push(...matrix.shift());
    for (let row of matrix) {
      if (row.length) result.push(row.pop());
    }

    if (matrix.length) {
      result.push(...(matrix.pop().reverse()));
    }

    for (let i = matrix.length - 1; i >= 0; i--) {
      if (matrix[i].length) result.push(matrix[i].shift());
    }
  }

  return result;
}

console.log(spiralOrder([
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9]
])); 

------------------------------------------------------------------------------
21.peak element

const arr=[1, 2, 3, 1]

for (let i = 0; i < arr.length; i++) {
    const left = i === 0 ? -Infinity : arr[i - 1];
    const right = i === arr.length - 1 ? -Infinity : arr[i + 1];

    if (arr[i] > left && arr[i] > right) {
        console.log(arr.indexOf(arr[i])); 
    }
}
--------------------------------------------------------
22. Check if Array is a Rotation of Another

const arr=[1,2,3,4,5].join(',')
const arr2=[3,4,5,1,2].join(',')


console.log((arr +',' +arr).includes(arr2))

--------------------------------------------------------------
23. Find First Non-Repeating Element

const arr=[9,4,9,6,7,4]

const res=arr.find((ele)=>arr.indexOf(ele)===arr.lastIndexOf(ele))

console.log(res)
-----------------------------------------------------------------------------------
24. Sort Array by Frequency (most frequent first)

const arr=[2,3,1,3,2]

const obj={}

for(let i of arr){
    obj[i]=(obj[i]||0)+1
}

const sorted=arr.sort((a,b)=>{
    if(obj[a]!==obj[b]) return obj[b]-obj[a]
    return a-b
})
console.log(sorted)

----------------------------------------------------------------------

25. Rearrange Alternate Positive and Negative

const arr=[1,2,-3,-4,5,6,-7]

const a=arr.filter(ele=>ele>0)
const b=arr.filter(ele=>ele<0)
let res=[]
while (a.length || b.length){
    if(a.length) res.push(a.shift())
     if(b.length) res.push(b.shift())
}
console.log(res)
------------------------------------------------------
26. Sort 0s, 1s, and 2s (Dutch National Flag)

const arr=[2,0,2,1,1,0]
let zero=0
for(let i=0;i<arr.length;i++){
    if(arr[i]===0){
        [arr[i],arr[zero]]=[arr[zero],arr[i]]
        zero++
    }
}

let one=zero

for(let i=zero;i<arr.length;i++){
     if(arr[i]===1){
        [arr[i],arr[one]]=[arr[one],arr[i]]
        one++
    }
}

console.log(arr)
-------------------------------------------------------------
27. Next Greater Element

const arr=[4,5,2,10]
const res=[]
for(let i=0;i<arr.length;i++){
    let found=false
    for(let j=i+1;j<arr.length;j++){
        if(arr[j]>arr[i]){
            res.push(arr[j])
            found=true
            break;
        }
    }
    if(!found) res.push(-1)
}

console.log(res)

--------------------------------------------------------------------------
28. Leaders in Array (Greater than all on right)

const arr=[16,17,4,3,5,2]
let max=arr[arr.length-1]
const res=[]
res.push(max)
for(let i=arr.length-2;i>=0;i--){
        if(arr[i]>max){
            res.push(arr[i])
            max=arr[i]
        }
}

console.log(res.reverse()) //[ 17, 5, 2 ]

----------------------------------------------------------------
29. Maximum Profit from Stock (Buy/Sell Once)

const arr=[7, 1, 5, 3, 6, 4];
let max=0

for(let i=0;i<arr.length;i++){
    for(let j=i;j<arr.length;j++){
       if(arr[j]>arr[i]){
         const v=arr[j]-arr[i]
          max=Math.max(max,v)
       } 
    }
}

console.log(max)// bought for 1 & sold for 6 so 6-1=5

---------------------------------------------------------------
30. Max Profit with Multiple Transactions

const price=[7, 1, 5, 3, 6, 4];
let profit=0

for(let i=0;i<price.length;i++){
    if(price[i]>price[i-1]){
        profit+=price[i]-price[i-1]
    }
}

console.log(profit)
--------------------------------------------------------------
31. Trapping Rain Water

function trap(height){
    let left=0, right=height.length-1
    let leftMax=0,rightMax=0,water=0;
    
    while(left<right){
        if(height[left]<height[right]){
            leftMax=Math.max(leftMax,height[left])
            water+=leftMax-height[left]
            left++
        }else{
            rightMax=Math.max(rightMax,height[right])
            water+=rightMax-height[right]
            right--
        }
    }
    return water
}

console.log(trap([4, 2, 0, 3, 2, 5])); //9
----------------------------------------------------

32. Longest Substring Without Repeating Characters (Array of chars)

const str="abcabcbb"
let longest=''
let current=''

for(let char of str){
    let index=current.indexOf(char)
    if(index!==-1){
        current=current.slice(index+1)
    }
    current+=char
    if(current.length>longest.length){
        longest=current
    }
}

console.log(longest)
----------------------------------------------------------------------
33. Search in Rotated Sorted Array

function search(nums,target){
    let left=0,right=nums.length-1;
    
    while(left<=right){
        let mid=Math.floor((left+right)/2);
        if(target===nums[mid]) return mid;
        
        if(nums[left]<=nums[mid] ){
            if(target>=nums[left] && target<nums[mid]){
                right=mid-1
            }else{
                left=mid+1
            }
        }else{
             if(target>nums[mid] && target<=nums[right]){
                left=mid+1
            }else{
                right=mid-1
            }
        }
    }
    return -1
}

console.log(search([4,5,6,7,0,1,2], 0)); 

----------------------------------------------------
34. Find Index of First 1 in Infinite Binary Array

Input (implicit infinite array): [0,0,0,1,1,1...]
Output: 3

const arr = [0,0,0,1,1,1,1,1]; 
ma
let index = 1;

while (arr[index] === 0) {
    index *= 2;
}

for (let i = index / 2; i <= index; i++) {
    if (arr[i] === 1) {
        console.log(i); 
        break;
    }
}
---------------------------------------------------------------

35. Maximum Subarray Sum with at most K Elements

const arr = [1, 2, 3, 4, 5];
const K = 2;

let maxSum = -Infinity;

for (let i = 0; i < arr.length; i++) {
    let sum = 0;
    for (let j = i; j < Math.min(arr.length, i + K); j++) {
        sum += arr[j];        
        if (sum > maxSum) {  
            maxSum = sum;
        }
    }
}

console.log(maxSum); 
------------------------------------------------------------------
36. Merge and Sort Intervals

 if (intervals.length <= 1) return intervals;

    intervals.sort((a, b) => a[0] - b[0]); 
    const arr = [intervals[0]];

    for (let i = 1; i < intervals.length; i++) {
        let last = arr[arr.length - 1];
        let curr = intervals[i];

        if (last[1] >= curr[0]) {
            last[1] = Math.max(last[1], curr[1]);
        } else {
            arr.push(curr); 
        }
    }

    return arr;